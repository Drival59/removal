{% extends 'base.html.twig' %}

{% block body %}
    <br>
    <div class="container">

      <div style="max-width: 600px;margin:auto">
      <h2 class="color-removal">Créer un recrutement</h2><br />
        <div class="removal-backform">
          {{ form_start(form) }}
          {{ form_errors(form) }}
          <div class="form-group">
            {{ form_label(form.image) }}
            {{ form_widget(form.image) }}
          </div>
          <div class="form-group">
            {{ form_label(form.classe) }}
            {{ form_widget(form.classe) }}
          </div>
          <div class="form-group">
            {{ form_label(form.niveau) }}
            {{ form_widget(form.niveau) }}
          </div><br />
          <div class="btn-outer" style="max-width:200px;margin:auto;">
            {{ form_widget(form.save) }}
          </div>
            {{ form_rest(form) }}
            {{ form_end(form) }}
        </div>
      </div>
    </div>
{% endblock %}
