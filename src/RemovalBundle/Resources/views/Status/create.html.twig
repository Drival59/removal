{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/style-participation.css') }}">
{% endblock %}

{% block body %}
    <br>
    <div class="all_form_status">
    <div class="status_create">
    {{ form_start(form) }}
    {{ form_errors(form) }}
    <div class="form-group">
        {{ form_label(form.confirmation) }}
        {{ form_widget(form.confirmation) }}
    </div>
    <div class="form-group">
        {{ form_label(form.participation) }}
        {{ form_widget(form.participation) }}
    </div>
    <div class="form-group">
        {{ form_label(form.utilisateur) }}
        {{ form_widget(form.utilisateur) }}
    </div>
    <div class="form-group">
        {{ form_label(form.status) }}
        {{ form_widget(form.status) }}
    </div>
        {{ form_rest(form) }}
        {{ form_end(form) }}
    </div>
    </div>
{% endblock %}