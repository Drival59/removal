{% extends 'base.html.twig' %}

{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('css/style-gestion.css') }}">
{% endblock %}

{% block body %}
    <br>
  <div class="container">
    <div class="create_character">
      <div class="removal-backform">
        <div class="title_creation_personnage">
          <h2>Création d'un personnage en cours</h2>
        </div>
        <br>
        {{ form_start(form) }}
        {{ form_errors(form) }}
        <div class="form-group">
        {{ form_label(form.pseudo) }}
        {{ form_widget(form.pseudo) }}
        </div>
        <div class="form-group">
        {{ form_label(form.classe) }}
        {{ form_widget(form.classe) }}
        </div>
        <div class="form-group">
        {{ form_label(form.specialisation) }}
        {{ form_widget(form.specialisation) }}
        </div><br />
        <div class="btn-outer" style="max-width:200px;margin:auto">
          {{ form_widget(form.save) }}
        </div>
        {{ form_rest(form)}}
        {{ form_end(form) }}
      </div>
    </div>
  </div>
{% endblock %}
